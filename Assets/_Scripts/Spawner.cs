using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spawner : MonoBehaviour
{
    public GameObject[] blockShapes;

    void Start()
    {
        
    }

    void Update()
    {
        //int ran = Random.Range(0, blockShapes.Length);
        //Instantiate(blockShapes[ran]);
    }
}
